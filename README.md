# MSP Unity SDK

Independent Unity plugin repository for MSP.

## Scope (MVP)

- Interstitial only
- Keep MSP public API semantics unchanged:
  - `Initialize`
  - `LoadAd`
  - `GetAd`
  - `Show`

## Status

- Git repo initialized locally
- UPM-style structure created
- Android/iOS native bridge skeleton added
- Editor mock flow available for local demo
